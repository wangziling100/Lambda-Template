{
    "actions":[
        "sam build",
        "cd {{.AppPath}}",
        "yarn install",
        "yarn test",
        "cd ..",
        "sam local invoke \"{{.AppName}}Function\" -e events/event.json --env-vars env.json",
        "sam deploy"
    ]
}
